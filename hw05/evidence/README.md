# Evidence checklist

Không đặt ảnh AI-generated hoặc ảnh từ HW02/HW04 vào đây. Mỗi ảnh phải được
chụp trong lần chạy HW05 thật.

| Thư mục | Evidence bắt buộc |
|---|---|
| `load/` | JMeter/terminal và htop/Task Manager cùng frame |
| `stress/` | JMeter/terminal và htop/Task Manager cùng frame |
| `spike/` | JMeter/terminal và htop/Task Manager cùng frame |
| `endurance/` | Tải duy trì và resource monitor; số liệu threshold |
| `hardware/` | dxdiag hoặc công cụ tương đương, hostname và cấu hình |

Tên ảnh đề xuất: `<scenario>_<timestamp>_<description>.png`. Không chỉnh sửa số
liệu trên ảnh. Ghi timestamp, lệnh chạy và file JTL tương ứng trong báo cáo.

